## Workflow

Ticket work happens on its own branch, pushed as a PR. Never commit or push directly to `main`.

## Agent skills

### Issue tracker

Issues live in GitHub Issues (`Dulain-Willis/study-helper`), via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout: `CONTEXT.md` + `docs/adr/` at repo root. See `docs/agents/domain.md`.
