[project]
name = "study-helper-mcp"
version = "0.1.0"
description = "FastMCP server for study-helper"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=3.3.1",
    "psycopg2-binary>=2.9.12",
]
